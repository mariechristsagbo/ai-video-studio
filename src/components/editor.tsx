"use client";
import { useEffect,useState,useCallback } from 'react';
import { VideoPlay,Magicpen,ExportSquare,Refresh,ArrowUp2,ArrowDown2,Copy,Trash,TickCircle } from 'iconsax-react';
import { api } from './client-api';
import { Button } from './ui/button';
import { Card } from './ui/card';
import { Badge } from './ui/badge';
import { Input,Textarea } from './ui/input';
import { progress } from '@/domain/video';
import type { detail,Shot } from '@/generations/repository';
type Detail=Awaited<ReturnType<typeof detail>>;
type Action=(path:string,body:unknown)=>Promise<void>;
export function Editor({id}:{id:string}){
 const [data,setData]=useState<Detail>(),[selected,setSelected]=useState<string>(),[tab,setTab]=useState('storyboard'),[error,setError]=useState(''),[busy,setBusy]=useState(false),[final,setFinal]=useState(false);
 const load=useCallback(async()=>{const d=await api<Detail>(`generations/${id}`);setData(d);setSelected(s=>d.shots.some(shot=>shot.id===s)?s:d.shots[0]?.id);},[id]);
 useEffect(()=>{let active=true;const tick=()=>{if(active)void load().catch(e=>setError(e.message));};tick();const timer=setInterval(tick,5000);return()=>{active=false;clearInterval(timer);};},[load]);
 const act:Action=async(path,body)=>{setBusy(true);setError('');try{await api(`generations/${id}/${path}`,body);await load();}catch(e){setError((e as Error).message);}finally{setBusy(false);}};
 if(!data)return <>{error&&<div className="error">{error}</div>}<div className="loading"/></>;
 const {generation:g,shots,scenes,renders}=data,shot=shots.find(s=>s.id===selected),completed=shots.filter(s=>s.clipId).length,failed=shots.filter(s=>['FAILED','UNCERTAIN'].includes(s.status)).length,queued=shots.filter(s=>s.status==='QUEUED').length,generating=shots.filter(s=>['SUBMITTING','SUBMITTED','GENERATING'].includes(s.status)).length;
 const latest=renders.find(r=>r.id===g.finalRenderId&&r.status==='COMPLETED')||renders.find(r=>r.status==='COMPLETED'),preview=(final?latest?.assetId:shot?.clipId)||latest?.assetId;
 const locked=['PLANNING','RENDERING'].includes(g.status),edit=(body:unknown)=>act('edit',{revision:g.revision,...body as object});
 return <><header className="page-top editor-top"><div><div className="eyebrow">Generation workspace</div><h1>{g.title}</h1><div className="project-subline"><Badge status={g.status}/><small>{g.aspectRatio} · {g.platform} · {g.targetDuration}s target</small></div></div><div className="editor-actions"><Button variant="outline" disabled={busy||locked||!shots.length} onClick={()=>void act('generate',{})}><Magicpen size={16}/>Generate missing shots</Button><Button disabled={busy||locked||!shots.length||completed!==shots.length||generating>0||queued>0} onClick={()=>{setFinal(true);void act('render',{});}}><VideoPlay size={16}/>Render video</Button>{latest?.assetId&&<Button asChild variant="outline"><a href={`/api/media/${latest.assetId}?download=1`}><ExportSquare size={15}/>Download</a></Button>}</div></header>
 {error&&<div className="error" role="alert">{error}</div>}{g.error&&<div className="error">{g.error}</div>}{g.status==='PLANNING'&&<div className="notice">Writing your brief, narration, and storyboard. This may take a few minutes. You can leave and return later.</div>}
 <div className="row spread"><small>{completed} / {shots.length} shots completed</small><small>{generating} generating · {queued} queued · {failed} failed</small></div><div className="progress-track"><div className="progress-fill" style={{width:`${progress(g.status,completed,shots.length)}%`}}/></div>
 <div className="tabs">{['storyboard','script & scenes','audio & captions'].map(t=><button className={`tab ${tab===t?'active':''}`} key={t} onClick={()=>setTab(t)}>{t[0].toUpperCase()+t.slice(1)}</button>)}</div>
 {tab==='storyboard'?<div className="editor-grid"><div><div className="preview">{preview?<video key={preview} src={`/api/media/${preview}`} controls preload="metadata"/>:<div className="preview-empty"><VideoPlay size={46} style={{margin:'auto'}}/><p>Your story, one shot at a time.<br/>Review the storyboard below, then generate your clips.</p></div>}</div><div className="preview-info"><small>{final?'Final render':shot?`Shot ${shot.position+1} preview`:'No preview yet'}</small>{latest&&<Button size="sm" variant="ghost" onClick={()=>setFinal(!final)}>{final?'Open storyboard':'Watch final video'}</Button>}</div><div className="timeline">{shots.map(s=><button key={s.id} className={`shot-card ${selected===s.id?'selected':''}`} onClick={()=>{setSelected(s.id);setFinal(false);}}><div className="shot-poster">{s.thumbnailId?<img src={`/api/media/${s.thumbnailId}`} alt={`Shot ${s.position+1}`}/>:<VideoPlay size={22}/>}</div><div className="shot-info"><div className="row spread"><strong style={{fontSize:12}}>Shot {s.position+1}</strong><small>{s.duration}s</small></div><Badge status={s.status}/></div></button>)}</div>{!shots.length&&g.status!=='PLANNING'&&<div className="empty"><p>No storyboard yet.</p><Button onClick={()=>void act('replan',{})} disabled={busy}>Retry planning</Button></div>}{failed>0&&<div className="notice">Completed clips are safe. <Button size="sm" variant="ghost" onClick={()=>void act('generate',{})} disabled={busy}>Retry failed shots</Button></div>}<div className="notice">Review factual claims and prompts before generating. Agnes bills each clip independently. A failed replacement never removes your previous clip.</div></div>{shot&&<ShotEditor key={`${shot.id}-${shot.updatedAt}`} shot={shot} data={data} busy={busy||locked} save={edit} act={act}/>}</div>:tab==='script & scenes'?<div className="max-readable"><Card><h2>Narration script</h2><p className="help">Edit before generating clips. Rebuild the storyboard to apply narration timing changes.</p><form key={`script-${g.revision}`} onSubmit={e=>{e.preventDefault();const form=new FormData(e.currentTarget);void edit({script:String(form.get('script'))});}}><Textarea name="script" defaultValue={g.script} style={{minHeight:250}} maxLength={30000}/><div className="row space-top"><Button disabled={busy||locked}>Save script</Button><Button type="button" variant="outline" disabled={busy||locked||completed>0} onClick={()=>{if(confirm('Rebuild the storyboard from the current saved script? Existing draft shots will be replaced.'))void act('replan',{});}}>Rebuild storyboard</Button></div></form></Card><details><summary>Creative brief & visual bible</summary><form key={`brief-${g.revision}`} onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{void edit({creativeBrief:JSON.parse(String(f.get('brief'))),visualBible:JSON.parse(String(f.get('bible')))});}catch{setError('Enter valid JSON for the brief and visual bible.');}}}><label className="field"><span>Creative brief (JSON)</span><Textarea name="brief" defaultValue={JSON.stringify(g.creativeBrief,null,2)} style={{minHeight:200}}/></label><label className="field"><span>Visual bible (JSON)</span><Textarea name="bible" defaultValue={JSON.stringify(g.visualBible,null,2)} style={{minHeight:160}}/></label><Button disabled={busy||locked}>Save creative direction</Button></form></details><h2 className="space-top">Scenes</h2>{scenes.map(scene=><form className="scene-card" key={`${scene.id}-${scene.updatedAt}`} onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);void edit({scene:{id:scene.id,title:String(f.get('title')),narration:String(f.get('narration'))}});}}><label className="field"><span>Scene {scene.position+1}</span><Input name="title" defaultValue={scene.title} required/></label><Textarea name="narration" defaultValue={scene.narration}/><Button size="sm" variant="outline" className="space-top" disabled={busy||locked}>Save scene</Button></form>)}</div>:<div className="max-readable"><Card><h2>Sound & subtitles</h2><p>One continuous narration track keeps your story coherent.</p><div className="upload-grid">{(['narration','music'] as const).map(kind=><div className="upload-box" key={kind}><strong>{kind==='narration'?'Narration recording':'Background music'}</strong><p className="help">{kind==='narration'?'The recording’s real duration drives the final timeline.':'Mixed quietly below narration.'} MP3, WAV, FLAC, OGG or MP4 · 50 MB max.</p>{(kind==='narration'?g.narrationId:g.musicId)&&<audio controls src={`/api/media/${kind==='narration'?g.narrationId:g.musicId}`} style={{width:'100%',height:32}}/>}<input aria-label={`Upload ${kind}`} type="file" accept="audio/*,video/mp4" disabled={busy||locked} onChange={e=>{const file=e.target.files?.[0];if(file){const f=new FormData();f.set('kind',kind);f.set('file',file);void act('upload',f);}}}/></div>)}</div><div className="notice">Agnes has no documented standalone narration API. Upload your voiceover, or use the estimated script timing. After uploading narration, rebuild the draft storyboard to rebalance shots before generating.</div><label className="toggle"><input type="checkbox" checked={g.burnCaptions} disabled={busy||locked} onChange={e=>void edit({burnCaptions:e.target.checked})}/>Burn captions into final video</label><label className="toggle"><input type="checkbox" checked={g.clipAudio} disabled={busy||locked} onChange={e=>void edit({clipAudio:e.target.checked})}/>Keep original clip audio at low volume</label><small>Captions use estimated phrase timing, high contrast, and a mobile-safe lower margin.</small></Card>{renders.length>0&&<Card className="space-top"><h2>Render history</h2>{renders.map(r=><div className="setting-row" key={r.id}><span>Version {r.version} <Badge status={r.status}/></span>{r.assetId&&<a className="text-link" href={`/api/media/${r.assetId}?download=1`}>Download MP4</a>}</div>)}</Card>}</div>}
 </>;
}
function ShotEditor({shot,data,busy,save,act}:{shot:Shot;data:Detail;busy:boolean;save:(body:unknown)=>Promise<void>;act:Action}){
 const active=['QUEUED','SUBMITTING','SUBMITTED','GENERATING'].includes(shot.status),disabled=busy||active;
 return <Card className="inspector"><div className="row spread"><h2>Shot {shot.position+1}</h2><Badge status={shot.status}/></div><form onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);void save({shot:{id:shot.id,data:{visualDescription:String(f.get('visualDescription')),videoPrompt:String(f.get('videoPrompt')),duration:Number(f.get('duration')),mode:String(f.get('mode')),transition:String(f.get('transition')),camera:String(f.get('camera')),environment:String(f.get('environment')),referenceId:f.get('referenceId')||null,characterId:f.get('characterId')||null,continuityFrom:f.get('continuityFrom')||null}}});}}><label className="field"><span>Visual description</span><Textarea name="visualDescription" defaultValue={shot.visualDescription} required maxLength={4000}/></label><label className="field"><span>Video prompt</span><Textarea name="videoPrompt" defaultValue={shot.videoPrompt} required maxLength={8000} style={{minHeight:150}}/></label><div className="form-grid" style={{gap:12}}><label className="field"><span>Seconds</span><Input name="duration" type="number" min={4} max={12} defaultValue={shot.duration} required/></label><label className="field"><span>Mode</span><select name="mode" className="input" defaultValue={shot.mode}><option value="text">Text</option><option value="keyframe">Keyframe</option><option value="reference">Reference</option></select></label></div><details><summary>Continuity & direction</summary><label className="field"><span>Camera</span><Input name="camera" defaultValue={shot.camera}/></label><label className="field"><span>Environment</span><Input name="environment" defaultValue={shot.environment}/></label><label className="field"><span>Character</span><select name="characterId" className="input" defaultValue={shot.characterId||''}><option value="">None</option>{data.characters.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label className="field"><span>Reference image</span><select name="referenceId" className="input" defaultValue={shot.referenceId||''}><option value="">None</option>{data.assets.filter(a=>a.kind==='reference').map((a,i)=><option key={a.id} value={a.id}>Reference {i+1}</option>)}</select></label><label className="field"><span>Continue from final frame</span><select name="continuityFrom" className="input" defaultValue={shot.continuityFrom||''}><option value="">No frame continuity</option>{data.shots.filter(s=>s.position<shot.position).map(s=><option key={s.id} value={s.id}>Shot {s.position+1}</option>)}</select></label><label className="field"><span>Transition to next shot</span><select name="transition" className="input" defaultValue={shot.transition}><option value="cut">Cut</option><option value="fade">Fade</option><option value="crossfade">Crossfade</option></select></label></details><Button className="full-width" disabled={disabled}><TickCircle size={16}/>Save changes</Button></form><label className="field space-top"><span>Upload a reference image</span><input type="file" accept="image/png,image/jpeg,image/webp" disabled={disabled} style={{fontSize:11,width:'100%'}} onChange={e=>{const file=e.target.files?.[0];if(file){const f=new FormData();f.set('kind','reference');f.set('file',file);void act('upload',f);}}}/></label>{shot.lastError&&<div className="error">{shot.lastError}</div>}<Button variant="outline" className="full-width" disabled={disabled} onClick={()=>{const uncertain=shot.status==='UNCERTAIN';if(uncertain&&!confirm('The previous request may have succeeded. Check your Agnes account first. Submit again and accept possible duplicate charges?'))return;void act('generate',{shotId:shot.id,acknowledgeUncertain:uncertain});}}><Refresh size={15}/>{shot.clipId?'Regenerate this shot':'Generate this shot'}</Button><div className="row space-top spread">{[['up',ArrowUp2,'Move up'],['down',ArrowDown2,'Move down'],['duplicate',Copy,'Duplicate'],['delete',Trash,'Delete']] .map(([op,Icon,label])=>{const I=Icon as typeof Trash;return <Button key={String(op)} type="button" variant="ghost" size="sm" title={String(label)} aria-label={String(label)} disabled={disabled} onClick={()=>{if(op==='delete'&&!confirm('Delete this shot from the storyboard?'))return;void save({operation:op,shotId:shot.id});}}><I size={16}/></Button>;})}</div></Card>;
}
