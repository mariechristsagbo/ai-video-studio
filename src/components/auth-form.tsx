"use client";
import { useState } from 'react';
import Link from 'next/link';
import { VideoPlay,Sms,ArrowRight } from 'iconsax-react';
import { Button } from './ui/button';
import { Input } from './ui/input';
import { Card } from './ui/card';
export function AuthForm({signup=false}:{signup?:boolean}){const [sent,setSent]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');return <div className="auth-page"><Card className="auth-card"><Link className="brand" href="/"><span className="brand-icon"><VideoPlay size={19} variant="Bold"/></span>Video Studio</Link>{sent?<><Sms size={40} color="#547442"/><h1 className="space-top">Check your inbox</h1><p>Your secure sign-in link is on its way. It expires in 10 minutes.</p><Button variant="outline" onClick={()=>setSent(false)}>Use another email</Button></>:<><div className="eyebrow">Your creative workspace</div><h1>{signup?'Create your account':'Welcome back'}</h1><p>Turn an idea into a story worth watching.</p><form onSubmit={async event=>{event.preventDefault();setBusy(true);setError('');const form=new FormData(event.currentTarget);try{const response=await fetch('/api/auth/sign-in/magic-link',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.get('email'),name:form.get('name')||undefined,callbackURL:'/dashboard',newUserCallbackURL:'/dashboard'})});if(!response.ok)throw new Error();setSent(true);}catch{setError('We could not send your link. Please try again or check email configuration.');}finally{setBusy(false);}}}>{signup&&<label className="field"><span>Name</span><Input name="name" autoComplete="name" placeholder="Your name" required maxLength={100}/></label>}<label className="field"><span>Email address</span><Input name="email" type="email" autoComplete="email" placeholder="you@example.com" required/></label>{error&&<div role="alert" className="error">{error}</div>}<Button disabled={busy}>{busy?'Sending link…':'Continue with email'}<ArrowRight size={16}/></Button></form><div className="auth-footer">{signup?'Already have an account?':'New here?'} <Link href={signup?'/sign-in':'/sign-up'}>{signup?'Sign in':'Create an account'}</Link><p className="space-top">No passwords. Just a secure link to your inbox.</p></div></>}</Card></div>;}
